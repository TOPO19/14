[phases.setup]
nixPkgs = ["php83", "php83Extensions.ctype", "php83Extensions.iconv", "php83Extensions.session", "php83Extensions.tokenizer"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t public"
